# へぇのタネ リリースチェックリスト

最終更新日: 2026年7月29日

## 1. 自動検証

- [x] `dart run tool/validate_content.dart`が成功する
- [x] `dart run tool/audit_content_sources.dart --require-approved`が成功する
- [x] レビューCSVのexport/import往復が成功する
- [x] manifestの問題数、カード数、公開可能数が実データと一致する
- [x] 問題とカードの出典URL・`contentHash`が一致する
- [x] 内容から再計算したSHA-256が承認時の`contentHash`と一致する
- [x] 公開対象画像のレビュー状態が`approved`または`generic_placeholder`
- [x] RC1の出題対象が直接出典監査済み47ペア(RC1ランタイム)で固定されている(編集済み70のうち23は監査保留)
- [x] `dart format --output=none --set-exit-if-changed lib test tool`が成功する
- [x] `flutter analyze`が成功する
- [x] `flutter test`が全件成功する
- [x] `flutter build web --no-web-resources-cdn`が成功する
- [x] `flutter build apk --debug`が成功する
- [x] 短期CI鍵による`flutter build appbundle --release`が成功する
- [x] CI鍵と`keystore.properties`が成果物・commitに残らない

自動検証はGitHub Actionsで継続実行する。2026年7月29日に、編集済み70ペア(stored)のうち47ペアを直接出典再監査し、semantic review、bundle hash、RC1件数ゲートを再検証した。

## 2. Android実機

- [ ] 初回起動からオンボーディング完了まで進める
- [ ] 今日の問題、回答ロック、正誤、解説、カード獲得が正常
- [ ] 同日再起動後に同じ問題・カードの完了状態が復元される
- [ ] コンテンツ更新後に別カードを回答済みとして表示しない
- [ ] カード一覧・詳細・出典リンクが正常
- [ ] カード閲覧だけで回答数・ストリークが増えない
- [ ] アプリ終了・再起動後もデータが保持される
- [ ] データリセット後にオンボーディングとテーマが初期化される
- [ ] 小型端末と大きな文字設定で操作不能にならない
- [ ] キーボード/外部入力で選択肢とカードを操作できる
- [ ] LINE、X、Google Drive等へ共有画像を渡せる
- [ ] 共有キャンセル後にエラーが表示されない
- [ ] 共有ボタン連打で複数の共有シートが開かない
- [ ] 配布署名済みAABをPlay Console内部テストへ登録できる

## 3. iPhone / iPad実機

Android v1.0の公開ブロッカーにはしない。iOS版着手時に確認する。

- [ ] 初回起動から主要フローを完了できる
- [ ] iPhoneで共有シートが正常に開く
- [ ] iPadで共有ポップオーバー位置が正常
- [ ] 共有中に画面を閉じてもクラッシュしない
- [ ] スワイプバック中に保存状態が壊れない
- [ ] Dynamic Typeで主要操作が欠けない
- [ ] TestFlightビルドをインストールできる

## 4. Web

Android v1.0の公開ブロッカーにはしない。Web公開時に確認する。

- [ ] Chrome、Safari、Edgeの最新安定版で主要フローを確認する
- [ ] Web Share API対応環境で共有できる
- [ ] 非対応環境でPNGダウンロードへフォールバックする
- [ ] 生成ファイル名が`hee_card_<card-id>.png`になる
- [ ] 画面幅320px程度でも主要操作ができる
- [ ] Tab、Enter、Spaceで主要操作ができる
- [ ] ホスティング環境でリロード時に404にならない

## 5. コンテンツ品質

- [x] 70問題を人が読み、問題文、正答、全誤答選択肢、解説が出典と整合する
- [x] 70カードのタイトル、短文、本文が出典と整合する
- [x] 全問題・カードに構造化された`source`情報がある
- [x] 出典の正式名称、発行元、個別HTTPS URL、確認日、根拠箇所が記録されている
- [x] `reviewStatus: approved`の組だけがリリース対象になっている
- [x] 承認後に本文を変更した2ペアは再承認されている
- [ ] 画像の内容適合性と利用権を確認している
- [x] 数値、ランキング、制度、現職者など変化する情報の確認日が妥当
- [x] 医療、法律、投資分野で断定的な助言をしていない
- [ ] 最終画像を含めて不適切表現、誤字、表示切れがない

## 6. ストア掲載

- [ ] アプリ名、短い説明、詳細説明を確定する
- [ ] アイコンとスクリーンショットを各端末サイズで用意する
- [ ] プライバシーポリシーを公開URLへ掲載する
- [ ] サポートURLまたは問い合わせ先を掲載する
- [ ] Google Play Data safetyを実装内容と一致させる
- [ ] 年齢区分をコンテンツに合わせて回答する
- [ ] バージョン番号とビルド番号を更新する
- [ ] リリースノートを用意する

## 7. Android v1.0一般公開条件

次をすべて満たすまでAndroid v1.0を一般公開しない。

- 自動検証が全件成功
- Android実機の主要フロー確認が完了
- 配布用署名とPlay Console内部テスト配布が完了
- 全70カードの最終画像と利用権の人手確認が完了
- プライバシーポリシー、サポート情報、ストア開示が実装と一致
- P0/P1の既知クラッシュ、データ損失、誤表示が0件
