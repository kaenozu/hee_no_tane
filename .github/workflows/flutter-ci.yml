name: Flutter CI

on:
  pull_request:

permissions:
  contents: write

jobs:
  format-source-audit:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout pull request branch
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Set up Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 # v2
        with:
          channel: stable
          cache: true

      - name: Format source audit fixes
        run: dart format lib/content_validation/source_auditor.dart test/content_validation/source_auditor_test.dart

      - name: Commit formatting
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No formatting changes required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lib/content_validation/source_auditor.dart test/content_validation/source_auditor_test.dart
          git commit -m "style: format source audit fixes"
          git push origin HEAD:${{ github.head_ref }}
